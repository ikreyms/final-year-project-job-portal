import { Box } from "@mui/material";
import React from "react";
import useStyles from "./styles";

const JobPosts = () => {
  const classes = useStyles();
  return <Box className={classes.panelWrapper}>h</Box>;
};

export default JobPosts;
